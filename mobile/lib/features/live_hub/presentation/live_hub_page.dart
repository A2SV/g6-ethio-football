import 'package:flutter/material.dart';

class LiveHubPage extends StatelessWidget {
  const LiveHubPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Live Hub Page')));
  }
}
