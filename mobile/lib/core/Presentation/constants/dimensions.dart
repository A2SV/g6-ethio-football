import 'package:flutter/material.dart';
// ------------------------------------
// PADDINGS AND RADIUS
// ------------------------------------
// Standard padding for content inside cards

const EdgeInsets kCardPadding = EdgeInsets.all(16.0);

// Standard padding for rows and columns
const EdgeInsets kScreenPadding = EdgeInsets.symmetric(
  horizontal: 12.0,
  vertical: 12.0,
);

const EdgeInsets kTop = EdgeInsets.fromLTRB(15.0, 10.0, 15.0, 0.0);

// Margin between clubs cards
const double kCardMargin = 6.0;

// Standard border radius for cards and buttons
const double kBorderRadius = 10.0;

// Standard icon size
const double kIconSize = 24.0;
